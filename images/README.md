Drop the hero photo of the venue here as:

    venue-hero.jpg

(Recommended: a wide/landscape photo, at least 1600px wide, so it looks sharp full-bleed behind the hero text. JPG keeps the file size reasonable for a fast-loading page.)

If your file has a different name or extension (e.g. `.png` or `.webp`), either rename it to `venue-hero.jpg`, or ask Claude to update the reference in `css/style.css` (search for `venue-hero.jpg`).

Until this file exists, the hero section just shows the plain champagne/rose gradient background — nothing breaks.
